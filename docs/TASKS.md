# Tasks — {{feature or batch name}}

> Copy this file per batch (`docs/tasks/<type>-<title>.md`). Any request with 4+ items
> goes here instead of a chat message — the file survives context compaction, nothing
> gets silently dropped, and each item gets verified before it's checked off.

**Branch:** `{{type/title}}`
**Definition of done for the whole batch:** {{one sentence}}

## Items

<!-- One item per line. `[x]` means VERIFIED done (evidence noted), not "probably done". -->

- [ ] 1. {{What}} — _accept when:_ {{observable criterion}}
- [ ] 2. {{What}} — _accept when:_ {{observable criterion}}
- [ ] 3. {{What}} — _accept when:_ {{observable criterion}}

## Verification log

<!-- Filled as items complete: item # → command/check run → observed result -->

## Dropped / deferred

<!-- Items consciously not done, with reason — so nothing disappears silently. -->
